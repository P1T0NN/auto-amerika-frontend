export interface typesUser {
    id: string;
    name: string;
    email: string;
    emailVerified: boolean;
    createdAt: string;
    updatedAt: string;
    isAdmin: boolean;
    country: string;
    phoneNumber: string;
    dateOfBirth: string;
}